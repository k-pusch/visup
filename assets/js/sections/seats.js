var SeatsSection = function (node, dataStore, geoStore) {
    BaseSection.call(this, node, dataStore, geoStore);
};

SeatsSection.prototype = Object.create(BaseSection.prototype);

SeatsSection.prototype.constructor = SeatsSection;
